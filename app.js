// Generated by CoffeeScript 1.3.3

/*
  ariuspay application
*/


(function() {
  var RUN_CONFS, VER, app, config, db, express, http, qs, url, util, _ref,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  VER = "ariuspay 0.0.1";

  console.log(("" + VER + " starting at: ") + new Date);

  RUN_CONFS = ["development", "production"];

  util = require('util');

  if (!process.env.NODE_ENV) {
    process.env.NODE_ENV = 'development';
  } else {
    if (_ref = process.env.NODE_ENV, __indexOf.call(RUN_CONFS, _ref) < 0) {
      console.error("NODE_ENV not in " + util.inspect(RUN_CONFS));
      process.exit(1);
    }
  }

  config = require('config');

  express = require('express');

  db = config.datasource.db;

  http = require("http");

  url = require("url");

  qs = require("querystring");

  app = express.createServer();

  app.configure(function() {
    app.set("views", __dirname + "/views");
    app.set("view engine", 'jade');
    app.set("view options", {
      layout: false
    });
    app.use(express.bodyParser());
    app.use(app.router);
    return app.use(express["static"](__dirname + "/inc"));
  });

  app.configure("development", function() {
    return app.use(express.errorHandler({
      dumpExceptions: true,
      showStack: true
    }));
  });

  app.configure("production", function() {
    return app.use(express.errorHandler());
  });

  app.get("/", function(req, res) {
    return res.redirect("/payment/manager/");
  });

  app.get("/payment/manager/", function(req, res) {
    return res.send("manager");
  });

  app.listen(config.http.port);

}).call(this);
